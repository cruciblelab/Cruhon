version: "3.9"

services:
  support-tawk:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./config.yml:/app/config.yml:ro
      - ./data:/app/data
      - ./uploads:/app/uploads
    restart: unless-stopped
    environment:
      - SUPPORT_TAWK_CONFIG=/app/config.yml
